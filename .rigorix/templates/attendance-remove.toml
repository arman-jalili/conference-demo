id = "attendance-remove"
name = "attendance-remove"
description = "RUN 1 (cross-run scene): the agent removes alice — a single action, within its own gate"
version = "1.0.0"
tags = ["conference", "cross-run"]

[[nodes]]
id = "registration_remove"
name = "registration_remove"
description = "Remove alice from conf-2026"
require_identity = true   # L1 identity gate (rigorix-oss F-20260907-05) — caller must present an attested claim or the run is refused at plan time. Inert until the engine honors it.
[nodes.action]
type = "run_command"
command = "bash .rigorix/scripts/remove_attendance.sh conf-2026 alice"
